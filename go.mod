// this is module name. Its usually recommended to be 
// unique if you are going to share it
// like with a domain name like github.com and org or user name
// its also recommendended to have only one module per repository
// multi comment syntax doesn't work here
module goplayground

go 1.24.3
