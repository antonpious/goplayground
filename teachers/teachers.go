// each package should be on its own folder
package teachers

import (
	"fmt"
)

// Only one main function is allowed
// the function names have special meaning
// with capital case
func GetTeachers() {
	fmt.Println("There are 10 Teachers")
}
